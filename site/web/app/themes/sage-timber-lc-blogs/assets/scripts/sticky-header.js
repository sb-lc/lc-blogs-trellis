(function($) {

	/*

    console.log( 'sticky-header.js' );

    var s = skrollr.init({
        edgeStrategy: 'set',
        forceHeight: false,
        smoothScrolling: false,
        easing: {
            WTF: Math.random,
            inverted: function(p) {
                return 1-p;
            }
        }
    });
    */

})(jQuery);